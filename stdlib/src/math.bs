# Change sign of number
neg(n) {
    return 0 - n;
}

# Absolute value
abs(n) {
    if n < 0 {
        return neg(n);
    }

    return n;
}

rand_range(min, max) {
    var range;
    range = max - min;
    return rand() % range + min;
}